{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import ./test/setup.mjs --test test/*.test.mjs",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
